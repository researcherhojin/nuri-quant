# Nuri-Quant — Work Backlog

이 문서는 **앞으로 할 일**만 기록한다. 완료된 항목은 git log + closed PR + closed issue 가 진실 source. 새 작업을 시작하기 전에 이 순서를 확인하고, 새 발견은 GitHub 이슈로 등록한 뒤 이 표에 추가한다.

정책 (자동 매매 영구 deferred, 작업 규칙) 은 `docs/STRATEGY.md §7` 에 있다. 이 문서는 operational backlog 만 담는다.

---

## Tier 1 — 완료 (2026-04-13 ~ 04-15)

| # | 항목 | 이슈 | PR | 비고 |
|---|------|------|----|------|
| 1 | **i18n constants extraction** | [#226](https://github.com/researcherhojin/nuri-quant/issues/226) | #230, #231 | `lib/strings.ts` 에 ~145개 한국어 상수 추출. 19 파일 마이그레이션 완료. |
| 2 | **하네스 계층화** | — | #229 | Fowler Guide/Sensor 기반 구조화: CLAUDE.md 슬림 (511→238줄) + 7 scoped CLAUDE.md + AGENTS.md + 4 hooks |
| 3 | **티커 기반 First-Run 온보딩 UX** | [#133](https://github.com/researcherhojin/nuri-quant/issues/133) | #234, #235 | `/explore` 페이지 + 티커 검색/분석 API. 커버리지 보강 포함. |
| 4 | **연 배당금 / 배당 수익률 데이터** | [#227](https://github.com/researcherhojin/nuri-quant/issues/227) | #270 | `dividendRate` (연 배당금 USD) + `dividend_yield_pct` (백분율) 컬럼 추가. fundamentals 테이블 마이그레이션 18-19. 2026-04-14 머지. |
| 5 | **Universe + Agent Data Coverage 통합 (P0)** | [#272](https://github.com/researcherhojin/nuri-quant/issues/272) | #275-#286 (12 PRs) | Audit에서 발견한 데이터 사일로 (fundamentals 2%) + universe label drift 해결. fundamentals 99%, prices 99%, KOSPI 200 100% 달성. 자동 검증 게이트 추가. |
| 5a | ↳ Phase 2a `universe_sync` | — | #276 | Wikipedia S&P 500 fetch + KRX/FDR KOSPI 200 fetch, manual ETF 보호 |
| 5b | ↳ Phase 2b BaseCollector `--source` | — | #278 | portfolio/universe/all 모드. 9 collectors tqdm + N/A coverage 진단 |
| 5c | ↳ Phase 2c validate_universe + CI | — | #284, #286 | 7-check coverage gate, warning-only CI job |
| 5d | ↳ KR/yfinance 성능 + UX fix | — | #281, #283, #285 | KR collect 33초, yfinance 10-thread parallel, sequential delay |
| 6 | **Privacy scanner ticker+PnL pattern** | — | #289 | STRATEGY §4.4.1 ticker+PnL 사각지대 보완. `-NN% (SYMBOL)` 괄호형 + `SYMBOL ±NN%` 인접형 양 패턴 감지 (예시는 `docs/STRATEGY.md §4.4.1` 참조), `origin/main..HEAD` unpushed commit message 스캔 추가, `TICKER_FALSE_POSITIVES` 120개. PR #202 class 차단. |
| 7 | **Shell scripts 전수 shellcheck clean** | — | #290 | 16개 `.sh` (1,504 lines) → shellcheck 0 issues. `set -euo pipefail`, shebang 통일, 실제 버그 fix (trap SC2064, read -r, RSYNC_OPTS array), `make lint-sh` + CI job 추가 |
| 8 | **OpenAI gpt-5.4-nano LLM 리포트 (STRATEGY §4.4.3 Tier 2)** | — | #294 | Ollama 휴면 → OpenAI primary. STRATEGY §4.4.3 정책 개정 (Tier 2 + ZDR 필수). `chat_text()` + `OPENAI_ZDR_APPROVED` 게이트. fallback chain (OpenAI → llama.cpp → Ollama). **부수 fix**: flaky `test_collect_full_flow` `df.copy()` (#295), security-scan 5m→10m timeout, codecov/patch 커버리지 테스트 3개 보강 |
| 9 | **uv sync 충돌 해결 (fastapi <0.129 pin)** | [#277](https://github.com/researcherhojin/nuri-quant/issues/277) | #291 | openbb-core ↔ fastapi version conflict 해결. dependabot ignore 추가 |
| 10 | **KR `n/a (US-only)` 표시 개선** | — | #288 | US_ONLY_TABLES frozenset + `check_universe_coverage.py` + `validate_universe.py` detail. 수집 실패 vs 소스 한계 시각 구분 |
| 11 | **#272 Phase 3 (Eval): validate_universe + US_ONLY 회귀 테스트** | — | #296 | 20 tests: TestUsOnlyTables(4) + TestRunValidation/Print/Main/Fetch(11) + TestOutputFormat(5) |
| 12 | **#272 Phase 4 (UX): Dashboard coverage widget + `/api/coverage`** | — | #297 | `CoverageStatus` widget (5/5 PASS 헤더 + 5-col 테이블 + 소스 한계 footer). 14 tests (backend 5 + frontend 9) |
| 13 | **README drift sync** | — | #309 | collectors 24→26, LLM 우선순위 (OpenAI primary), test counts 2,763→2,934. Codex APPROVED. |
| 14 | **B2 — Learning Memory outcome read 역방향** | — | #310 | `_compute_weights` SELECT 에 `outcome_30d` 누락 + `sqlite3.Row.get()` 없음. 두 층 버그로 수개월간 weight 역방향. Fix + 3 regression (revert-proof). #308 lock-in 해제. |
| 15 | **B1 — recommendations UNIQUE + UPSERT** | — | #311 | `UNIQUE(date, ticker, action)` → `UNIQUE(date, ticker)`. Migration 20 (MAX(id) dedup). `INSERT OR REPLACE` → `ON CONFLICT DO UPDATE` (id 보존 — `trades.recommendation_id` FK 안전). 프로덕션 20 중복 그룹 정리. |
| 16 | **#248 SIEGE v2 Phase 1 — asset-class gates** | [#248](https://github.com/researcherhojin/nuri-quant/issues/248) | #312 | Gate 5/7/8 per-asset-class (us/kr_equity/kr_index/commodity/bond). Cross-market spillover (KOSPI+SPY, USD/KRW+VIX). `config/rules.yaml siege_gates` spec. Codex challenge (3 결함 지적) → 재설계 → APPROVED. |
| 17 | **#89 인터랙티브 백테스트 sliders + live equity curve** | [#89](https://github.com/researcherhojin/nuri-quant/issues/89) | #332 | `run_interactive_backtest()` — regime change arm, stop/tp threshold hit 시 disarm. `/swing/backtest/equity` 가 `sma`/`period`/`sl`/`tp` 쿼리 수락 + 5분 TTL cache. UI 4-param sliders + URL deep-link (`/strategy?sma=100&lb=3Y&sl=-10&tp=30`) + static/interactive toggle. Tier 2 P1 #7 완료. |
| 18 | **wallstreet collect 성능 검증** | — | #285 | `make collect-universe` 에서 wallstreet 50min → 15min 41초 실측 확인 (universe 746). Tier 2 P2 #8 완료. |
| 19 | **Phase 2 A-1a — LM read path fix** | — | #361 | `_compute_weights` 가 `signals.verdicts` 대신 `agent_verdicts` 컬럼을 읽도록 수정. 133 rows silent fallback 해결. 5+3 regression (malformed JSON race, min_records parsed-gate, observability log level). Codex 2 round PASS. |
| 20 | **Phase 2 scheduler automation** | — | #363 | 스케줄러에 `consensus` job (07:05 KST) 추가. technical 07:00 완료 후, daily_report 08:00 전. LM input 데이터를 매일 자동 누적 → 2026-05-17± 첫 weight shift 예상. |
| 21 | **Phase 2 A-2a — consensus scoring_detail persist** | — | #364 | `save_to_recommendations` 의 hardcoded `scoring_detail=None` 제거. `_build_consensus` 가 per-agent weight × confidence contribution breakdown 계산, JSON 직렬화해 persist. Schema: `source`/`schema_version` discriminator + `basis_action` + `final_action_source` (weighted_sum/risk_veto/divergence_penalty). A-2b/c 가 consume 할 contract 완성. Codex 2 round (HIGH/MEDIUM/LOW + 1 residual gap → 모두 fix). |
| 22 | **Phase 2 A-2b-pre — candidates discriminator + VIX sync** | — | #366 | A-2a Round 2 follow-up. candidates.py scoring_detail 에 `source="candidate"` + `schema_version=1` 추가 (consensus 와 구분). VIX blocked/caution 경로에서 `scoring_detail["final_confidence"]` + `vix_penalty` 업데이트 — `c.confidence` 와 audit trail stale 방지. tracker.py falsy guard → `is not None` (empty dict `{}` persist). 4 regression lock. Codex 2 round (Medium VIX fix + Round 2 no findings). |
| 23 | **Phase 2 A-2b — API expose scoring_detail + agent_verdicts** | — | #368 | 3 routes (`/consensus`, `/dashboard`, `/actions`) 에 scoring_detail + agent_verdicts pass-through. `_parse_json_field` helper + graceful NULL/malformed degrade. `_build_actions` 에서 urgent/check/hold bucket 전부 threading. Pylance pre-existing type error fix 포함. 8 regression test (consensus 2 + dashboard 2 + actions 3 + endpoint 1). Codex 2 round (HIGH `_build_actions` drop → fix, Round 2 no findings). CI Shard 1 `external_analysis` table 누락 mock fix 포함. |
| 24 | **Phase 2 A-2c — ConsensusTable scoring_detail UI** | — | #370 | `/consensus` 페이지에서 final_action_source 이모지 배지 (🛑 risk_veto / ⚠️ divergence_penalty) + basis_action ≠ final 시 "BUY → HOLD" 라벨 + expanded row agent card 에 weighted contribution % (`action_scores[basis_action]` 분모) + counted_for_basis_action emerald border. TS literal union (`Action`/`ScoringSource`/`FinalActionSource`) export + `SOURCE_META satisfies` 로 contract drift 방어. 7 regression lock. Codex 2 round (LOW 2건 causal share denominator + contract drift → fix, Round 2 LOW 1건 tooltip 수식 → fix). Live API 21 tickers verify. CI TS type-check fail (makeScoring mock 반환 string widen) → `Partial<ScoringDetail>` annotate fix. |
| 25 | **Phase 2 A-1b — rows_parsed gate tighten** | — | #372 | A-1a Round 2 residual P2 close. `_compute_weights` 가 HOLD-only row 를 `rows_parsed` 에서 제외 — 10 HOLD-only row 가 `min_records=10` gate 통과하던 silent fallback 차단. `rows_skipped_no_usable` 카운터 (codex 넓은 semantic: HOLD-only + 미등록 agent + 이상 action 값 포함). WARNING/INFO log 에 surface. 3 regression lock (fallback + anomaly INFO path). Codex 2 round (no blocking → 2 LOW rename + observability gap + Round 2 stale comment → 전부 fix). NEXT_SESSION drift 발견: agent_accuracy_snapshots job 이미 scheduler Sunday 08:00 등록돼 있음 — spec line stale. |
| 26 | **Phase 2 A-3 — unified sell-path stop_loss** | 2026-04-18 | #374 | 3 sell path (risk_agent / actions / certification) 가 각각 다른 threshold + operator 를 썼음 — risk_agent `STOCK_STOP_LOSS`(-7 global) + `<=`, actions 하드코딩 `< -7`, certification 만 per-account strategy + `<`. long_term(-20)/pension(-30) 계좌 보유 ticker 가 -7% 손실에서 잘못 urgent/breach 분류. Fix: `nuri/core/rules.py::get_stop_loss_for_account(account)` helper + callers 가 pnl 계산된 holding row 의 account 를 helper 에 전달 (codex Round 1 P1 aggregation mismatch 방지). Operator 도 `<` 로 통일 (codex P2). 15 regression test (helper 7 + risk_agent 4 + actions 4). Codex Round 1: P1 + P2 모두 fix. Round 2 hung → self-review debt (§4.3) + 다음 PR 에서 회수. 3016/3016 test-fast pass. |
| 27 | **chore(tooling) codex_review.sh wrapper** | 2026-04-18 | #376 | `scripts/codex_review.sh <pr> <round> "<prompt>"` — `codex exec` 래퍼 + 프롬프트/응답을 markdown 으로 archive. Raw rollout JSONL (`~/.codex/sessions/`) 은 canonical source, wrapper 는 PR-scoped human-readable excerpt. STRATEGY §4.3 debt recovery 용. gitignored. Shell Lint SC2320 ($? misuse) fix 포함. |
| 28 | **Phase 2 A-4 — catalyst requirement for non-emergency SELL** | 2026-04-18 | #377 | Non-breach SELL 이 "왜 지금?" 근거 없이 surface 되는 걸 차단. `nuri/core/catalyst.py::has_recent_catalyst(ticker)` — 14d ticker news OR 7d 유의미 macro_event (conf≥0.5, \|sent\|≥0.3). `candidates.py` 는 SELL + actionable 을 catalyst 없으면 `TIER_ADVISORY` 로 downgrade. `actions.py _build_actions` 는 non-breach SELL 을 catalyst 없으면 `hold` 로. **stop-loss breach 는 exempt** — §2.2 mechanical, `pnl < stop_loss_threshold` path 가 catalyst 호출 전에 early continue. §2.6 Soft penalty. **Bonus**: codex Round 1-2 debt recovery — risk_agent 의 `holding[0]` arbitrary row 를 per-row iterate + concentration sum-over-rows 로 수정. Round 3 clean. 14 new test. 3030/3030 pass. CI 3회 재시도 (Shell Lint / Doc Count / `has_recent_catalyst` unmocked). |
| 29 | **Phase 2 A-5 — intraday live oracle divergence flag** | 2026-04-18 | #379 | Stored `prices` 는 T-1 → 장중 recommendation 이 stale 값으로 판단 (NFLX 사례: stored $97.23 vs live $107.79, +10.87%). `nuri/core/live_price.py` — `fetch_live_price(ticker)` yfinance `fast_info.last_price` (시장외 / weekend / 예외 → None) + `check_divergence(ticker, stored, threshold=3.0)` `(is_divergent, pct, live)` 반환. `actions.py _build_actions` 는 per-item `live_price`/`divergence_pct`/`divergence_flag` + `⚠ 실시간 시세 divergence …` reason. **threshold 비교는 stored 사용 유지** — STRATEGY §2.6 Surface 단계. A-5b 에서 live 를 stop-loss/target 비교로 승격 예정. 22 new test (MarketHours 9 + Fetch 4 + CheckDiv 7 including exact-3% boundary + Actions integration 3). Codex Round 1: P2 (Mon 05:00 KST wrap-around bug — 전날 weekday 기반으로 판정) + LOW (operator `>` → `>=`). Round 2 CLEAN + docstring nit fix. |
| 30 | **Phase 2 A-6 — simplicity audit (actions aggregation + defensive getattr drop)** | 2026-04-18 | #381 | `_get_portfolio_map` 이 동일 ticker 의 여러 계좌 보유를 largest-position row 로 collapse 했던 것을 aggregation 으로 수정 — `position_pct` 합산, `pnl_pct`/`account` 는 worst-pnl row (A-4 codex Round 1-3 재발 flag close). Pension 계좌는 aggregation 에서 분리 (codex Round 1 P2 추가 fix — 혼합 ticker 에서 taxable slice 가 pension label 로 skip 되는 cross-contamination 방지). `Candidate.tier` dataclass default 이므로 4 call sites 의 `getattr(c, "tier", …)` 제거 + 7 MockCandidate 에 `tier` field 추가 — defensive lookup 이 test fixture 의 real-shape drift 를 숨기고 있었음. 6 regression (aggregation 4 + pension split 2). Codex 2 round CLEAN + pension fix. 3060/3060 pass. CI Privacy Leak Scan 한 번 fail — cSpell whitelist 에 broker name stems 추가했던 것이 scanner 에 잡혀 정확히 의도대로 차단 (§4.4.1), 해당 entries 제거하고 재push. |
| 31 | **E2 — Position-sizing theoretical foundation (Kelly/Markowitz/Faber + Symmetric amplifier + SIEGE limits hypothesis)** | 2026-04-18 | #391 | docs-only foundation for E3 regime-adaptive framework. STRATEGY §3.4 에 3 학술 인용 (Kelly 1956 framework + MacLean/Thorp/Ziemba 2011 fractional, Markowitz 1952 efficient frontier, Faber 2007 trend-following) — full bibliography + DOI. §2.6 Escalation Ladder 에 4번째 rung "Symmetric amplifier" (post-veto sizing, Hard veto always wins on conflict). §3.6 E3 placeholder + acceptance preview (baseline = current SIEGE, horizon = `outcome_30/60/90d`, asset-class benchmarks SPY/KOSPI/KRX300/GC=F, walk-forward 5Y/1Y, gates Sharpe+0.1 / Max DD ±5% / Monte Carlo). §3.7 SIEGE limits 진단 (live count `ALL_CERT_CHECKS` 11 base + per-class expansion, all downside-block — upside/opportunity-cost gate 0개) labeled hypothesis-grade. Sequencing: 사용자 페인 "보수적 결론" 의 구조적 원인은 sizing-rule legitimacy → 🅑→🅒→🅐 (codex Plan consult APPROVE: docs spike → framework → rebalance under new rules). Plan-stage codex consult 1 round (item #5 SIEGE_V2 "Future direction" cut, single source of truth STRATEGY §6/§3.7). Round 1 MODIFY (3 P1: Kelly source-claim mismatch, Markowitz overstrong, "16 conditions" 사실오류) + 4 lower (Faber bib, amplifier order, §3.7 prescriptive drift, E3 executability) → Round 2 APPROVE. 3060/3060 test-fast. Archive: `codex-reviews/E2-round[1-2]-20260418-235007-*.md`. |
| 32 | **E3-0 — STRATEGY §3.6 acceptance method restate (3-stage validation + paired metrics)** | 2026-04-19 | #393 | E3 진입 시 live measurement 으로 #391 §3.6 acceptance preview 가 데이터 부재로 불가능함 발견 — `recommendations.outcome_30/60/90d = 0/156`, `regime_transitions = 1 row`, `regime_classifications` table 부재. SPY prices 5Y 만 OK. 어제 ship 한 "walk-forward 5Y/1Y / Sharpe +0.1 / Max DD ±5% / Monte Carlo" 가 phantom acceptance — STRATEGY §5.5 (Test Illusion) + NEXT_SESSION 교훈 #4 (NEXT_SESSION 도 stale 가능) 의 직접 적용 사례. Codex Plan consult on E3 validation method (5 candidates A/B/C/D/E 비교 — vectorbt walk-forward / phase split / defer / counterfactual / two-stage) → **E + Stage 0 (3-stage)** 채택. Stage 0 = no-lookahead audit (hard gate, classifier `compute_dynamic_thresholds` / `_load_spy_series` / special detectors / monthly sampling 의 future row 유입 검증), Stage 1 = classifier plausibility (diagnostic — sizing 은 loss attenuation 으로 가치 추가 가능, label predictivity 약해도 ship 가능), Stage 2 = paired counterfactual (main gate — frozen entry signal, N≥200 historical, same entries/prices, only sizing differs). Primary metric swap: bootstrap CI on mean/median paired delta + downside rate / MAE / CVaR (Sharpe Δ0.1 demote — 50-200 sample 통계 power 부족, sign test secondary). Walk-forward + Sharpe 는 "Secondary (insufficient history 로 차단 — abandon 아님)". §3.7 hypothesis 도 paired outcomes 로 soften. Codex Round 1 MODIFY (2 P1: Stage 1 hard-gate 오류, entry-source mixing) + 4 lower (sign test priority, §3.7 prescriptive drift, "Secondary" wording, single-source freezing) → Round 2 MODIFY (intro "모두 통과 시 ship" vs Stage 1 demotion 모순) → Round 3 APPROVE. E3 sub-task 분해: E3-0 (이 PR) → E3-1 (Stage 0 audit code+test) → E3-2 (Stage 1 plausibility script+docs) → E3-3 (Stage 2 + config + code 본체). 총 3-4 세션 (NEXT_SESSION 가정 2-3 보다 길어짐 — 정직함 확보 비용). 3060/3060 test-fast. Archive: `codex-reviews/{plan-e3-validation,E3-0-round[1-3]}-20260419-*.md`. |
| 33 | **E3-1 — Stage 0 classifier no-lookahead audit (11 tests)** | 2026-04-19 | #395 | E3-0 STRATEGY §3.6 의 Stage 0 hard ship gate 구현. `tests/quant/test_regime_no_lookahead.py` 신설 — `nuri/quant/regime/classifier.py` 의 historical 호출이 미래 데이터를 사용하지 않음 11 test 로 검증. Audit target 4 surface: thresholds (compute_dynamic_thresholds VIX median + SMA gap stddev — 2 test) / data loaders (_load_spy_series rows + _get_vix + _get_fear_greed — 3 test) / DB-backed special detectors (_detect_stagflation CPI/GDP + _detect_sector_rotation SPY/XLK 21d — 2 test) / monthly sampling (classify_regime_history 가 last business day per month 정확 pinning, 2024-01-31/02-29/03-29 — 1 test). End-to-end invariance class (3 test): classify_regime + classify_regime_history + _detect_sector_rotation 가 cutoff 이후 future row 추가 전후로 동일 (any leak shape 검출). Pure 함수 (_detect_euphoria, _detect_recovery) 는 호출자 책임 — 데이터 로더 test 가 cover. **결론**: classifier 가 이미 lookahead 없음 (codex code inspection 가설을 11 hard regression test 로 lock-in). Codex Round 1 MODIFY (3 P1: monthly weak / sector single-shape / e2e missing) → Round 2 MODIFY (1 P1: history e2e missing) → Round 3 closed (codex 가 targeted test 직접 실행해 1 passed verify). Test-only PR. CI 1차 fail (Doc Count Drift Check — 139→140 files, 3083→3094 tests) → 2차 fix 도 main 에서 sync 하면서 file count 미반영 → 3차 PR branch 에서 정확 sync, MERGEABLE. 3094/3094 test-fast pass. Archive: `codex-reviews/E3-1-round[1-3]-20260419-*.md`. |
| 34 | **E3-2 — Stage 1 classifier plausibility (diagnostic, 6 smoke tests)** | 2026-04-19 | #398 | E3-0 STRATEGY §3.6 Stage 1 implementation. Diagnostic (NOT hard gate) — `scripts/stage1_classifier_plausibility.py` 신설 (174 lines). N=12 monthly samples (VIX 1Y window 한계, spec N=24~36 — VIX backfill 후 풀가동 가능) → 각 date regime label → forward 21-trading-day SPY return 분포. Live run distribution: bull_low_vol (n=2) +3.37% mean / sideways_low_vol (n=2) +3.90% / sideways_high_vol (n=1) +6.28% / sector_rotation (n=4) +1.19% / **recovery (n=3) -1.52%, 33% positive — tentative anomaly** (small-n noise per codex Round 1, NOT credible regime failure, Stage 2 paired counterfactual review prompt). Methodology fixes (codex Round 1 P1 3건): (1) sample design recency-bias 명시 disclosure, (2) forward horizon 30 calendar days → exact 21 trading days (was 31-33 cal days ambiguous), `exit_date` column transparency, (3) Sampled N vs usable N_ret 분리 표시. 6 smoke test in 3 class (cutoff = SPY-21, exact +21 trading days + None on insufficient, output invariants — recency-bias / Sampled-vs-usable / exit_date column). importlib.spec_from_file_location for test load (Pylance Pylance reportMissingImports fix) + sys.modules registration (`@dataclass cls.__module__` lookup) + `patch.object(s1, "query")` (importlib loads s1 BEFORE patch — local binding bypass). Codex Round 1 MODIFY (3 P1 + 2 lower) → Round 2 MODIFY (1 P1 output invariant tests 누락) → Round 3 APPROVE. Data depth limitations: VIX 1Y / fear_greed 10 rows / CPI/GDP 0 rows. 3077/3077 test-fast pass. Doc counts synced (3094→3100, 141→142). Archive: `codex-reviews/E3-2-round[1-3]-20260419-*.md`. **다음**: 🅒 E3-3 Stage 2 paired counterfactual + config + code 본체 (main hard gate). |
| 35 | **E3-3a — Backfill VIX 5Y + prices 5Y for Stage 2 prerequisite** | 2026-04-19 | #400 | E3-3 Stage 2 main hard gate 의 데이터 prerequisite. `scripts/e3_3_backfill.py` 신설 (215 lines, idempotent). Codex Plan consult (E3-3-roundplan-r1) 4 결정: Q1 entry signal = per-ticker SMA 50/200 golden cross (single family, no entry-effect 오염), Q2 VIX = backfill 5Y (production classifier path 검증), Q3 universe = top 50-100 frozen + survivorship caveat, Q4 sizing rule = 3-bucket (1.2× / 1.0× / 0.8×) on per_position_max + max_sector_exposure only. Acceptance for full E3-3 Stage 2 PASS: 60d horizon 95% bootstrap CI lower bound of mean paired delta > 0.00% + downside rate ≤ baseline + 2pp 모든 horizon, 60d flat-or-better. Frozen universe = `config/universe.yaml us_core.tickers` (85 curated). **Live backfill**: VIX 258→1758 rows (period="5y" 1256 + 기존 더 넓은 7Y 유지, 2019-04-22 ~ 2026-04-17), Prices 85/85 fetched 103,318 rows upserted, 77/85 with full 5Y (8 partial = post-2021 IPOs ARM/DUOL/HOOD/NBIS/OKLO/RDDT/RIVN/TEM, codex 권장 "keep them" — exclude 시 universe 가 더 mature-survivor biased). Codex Round 1: no P1 blockers, 2 non-blocking fix (VIX `period="1825d"`→`"5y"` semantic safer, `pd.Timestamp.today()`→`today_kst()` 시간대 룰). 두 fix 모두 적용 + Round 2 CLEAN. Survivorship caveat: 오늘의 us_core 는 delisted ticker 제외돼 magnitude affect (direction 신호 무관 per codex framing) — Stage 2 report 에 explicit. Mac mini 는 이 PR merge 후 `python scripts/e3_3_backfill.py` 1회 실행 필요 (DB gitignored). 3077/3077 test-fast pass. CI 22/22 green, MERGEABLE 즉시. Archive: `codex-reviews/{PRE3-3-roundplan-r1, PR400-round[1-2]}-2026041[8-9]-*.md`. **다음**: E3-3b — entry generator + paired counterfactual sim + Stage 2 report (no config wiring). |
| 36 | **E3-3b — Stage 2 paired counterfactual sim — main hard gate PASS** | 2026-04-19 | #402 | E3-3 Stage 2 main hard gate 구현 + 실행. **Verdict: PASS** — regime-adaptive sizing 룰 정당화. `scripts/e3_3b_stage2_counterfactual.py` 신설 (322 lines) + `tests/scripts/test_e3_3b_stage2_counterfactual.py` (22 test in 5 class). Methodology: per-ticker SMA 50/200 golden cross × us_core 85 ticker × 5Y → 254 entries. Each entry: regime classify + baseline_size (15%) vs adaptive_size (1.2× aggressive / 0.8× conservative / 1.0× neutral) × forward 30/60/90d return → paired delta. Bootstrap CI (10000 iter, percentile, seed=42) + wrong_directional_pct + MAE + CVaR. **Live results**: 60d 95% CI = [+0.0153, +0.1588]% (primary PASS), wrong-dir 22-26% (≤55% PASS), 60d median ≥ 0 (sanity PASS), MAE_Δ -0.08~-0.14pp (adaptive worse — aggressive cases scale up MAE proportionally), CVaR_5%_Δ -0.87~-1.30%. Magnitude modest: ~+0.5%/year annualized. Entry distribution: recovery(70) + bull_low_vol(52) = 48% aggressive, neutral 41%, conservative 11%. **Self-correction during Build (caught before codex)**: 원안 codex "downside rate ≤ baseline+2pp" gate 가 single-position model 에서 mathematically trivial (adaptive_return 과 baseline_return 둘 다 positive_size × forward_return → same sign 항상). Reframe to `wrong_directional_pct` = P(paired_delta < 0). 의미: "did adaptive sizing **help** vs baseline?" NOT "did loss frequency worsen?". Loose gate (neutral entry == 0 mechanically improve) — MAE/CVaR 와 함께 읽어야 risk picture 완성. Codex Round 1 no P1 blockers (5 methodological challenges all addressed) → 2 non-blocking framing polish (semantic + neutral caveat) → Round 2 CLEAN + 1 residual (header text consistency 도 fix). 3122/3122 test-fast pass. Doc counts auto-synced (3100→3122). Codex 명확 verdict: "ship E3-3c **narrowly for per_position_max ONLY**, sector cap stays out until portfolio simulator exists". Archive: `codex-reviews/PR402-round[1-2]-20260418T17[42-45]*.md`. **다음**: E3-3c — `siege_gates.regime_overrides` config + `certification.py` regime branch + 15+ regression test (per_position_max 만, sector cap 제외 per codex). |
| 46 | **#454 — freshness gate dual-source (closed without code, debt note)** | 2026-04-28 | #459 | Post-#457 reassessment. **Codex Plan consult verdict (c)**: 옵션 A (prices+macro dual-source gate) reject — (1) data_fresh 는 의도적으로 prices-only, #457 freshness pass 가 SPY/TLT/GC=F 매일 prices 채움 → reported symptom 해소, (2) historical TLT/gold 소비자 0건 (`grep -rn "prices.*TLT" nuri/` empty) → "TLT depth" 정당화 evaporates, (3) **drift risk = split-brain** (gate PASS on macro.gold 37h-fresh while downstream reads stale prices."GC=F" → silent). 옵션 A 가 ops 더 위험. **Action**: code 변경 없이 close + `nuri/collectors/CLAUDE.md` §"Freshness Sentinel Redundancy (#453/#454, post-#457)" 9-line debt note (gold macro 5Y vs prices 1mo / TLT shallow / drift rationale). **gstack Flow 단계**: Think (issue + DB live state) → Plan (codex consult) → Plan gate 에서 (c) verdict → Build/Test/Review skip (codex challenge 가 review 역할) → Ship (#459 docs-only) → Reflect (이 row). Trivial chore inline 압축 (STRATEGY §2.7 trivial 정책). 1 file +9 lines, CI 9/9 PASS, codecov/patch PASS. Issue #453 + #454 합쳐 SIEGE freshness 5/5 PASS 라이브 검증 완료. |
| 45 | **#453 — stock collector freshness source** | 2026-04-28 | #457 | `make collect` (Phase A) 가 `--source portfolio` default 라 SPY/TLT/GC=F universe-only freshness ticker 영구 stale → SIEGE freshness gate dead → regime classifier 차단. Live state pre-fix: `prices.SPY` 8 days stale, `prices.TLT/GC=F` 0 rows. **Fix**: `nuri/collectors/stock.py` 에 `_load_freshness_tickers()` helper — `RULES.siege_gates.asset_classes.*.freshness_primary/secondary` single source of truth, `_NON_YFINANCE_FRESHNESS_TICKERS={"KOSPI"}` static blocklist (macro indicator name, #454 dual-source 범위). `StockCollector.collect(source="freshness")` 분기는 `BaseCollector._get_tickers()` 우회 (codex Plan consult: shared API에 SIEGE-specific 결합 회피). argparse `choices` 에 `freshness` 추가. **Makefile**: `collect:` 에 1-line freshness pass append. **Codex Plan consult** verdict (b) ship-with-changes — `BaseCollector` 로 끌어내리지 말고 stock.py scope 유지 + `freshness_secondary` 순회 keep + KOSPI static blocklist OK + scope (US-only) PR body 명시. **Codex Round 1 Review GATE: PASS**, no P1 (1 non-blocking: argparse choices 검증을 grep-based 로 한 점 — 라이브 verify 가 보강). 5 regression tests in `TestFreshnessSource` (extract primary+secondary, exclude KOSPI, empty config, branch routing, argparse literal). **Live verify**: `python -m nuri.collectors.stock --source freshness --period 1mo` → 3 tickers / 60 rows / 3.8s. SIEGE `_check_data_freshness` → us_equity (SPY 37h) / bond (TLT 37h) / commodity (GC=F 37h) 전부 PASS. **Bonus discovery**: KOSPI 가 `prices` 에 374 rows 존재 (stock_kr 가 채움) → kr_equity gate 도 우연히 PASS — issue #454 (TLT/GC=F 가 macro 테이블 아닌 prices 에서 lookup 되도록 dual-source 설계) 의 일부 redundant 가능성, 다음 세션 재평가. 492 collector tests pass + ruff clean. 1 commit, 3 files, +138/-4. CI 9/9 required check PASS. Branch: `fix-stock-collector-freshness-source` squash-merged + deleted. |
| 44 | **E4-0b v2 — 60-month measurement closure (honest rewrite, codex HOLD→GO)** | 2026-04-22 | #443 | Mac mini 60-month production rerun (PID 45898, 240 attempted → 141 binary-valid, `sector_concentrated` variant 0/60 unbuildable 후 codex Round 1 Unknown 제외로 real sector tag sparse 노출). **Measurement finding**: `position_limit` Δ30d=+2.45% (pilot +4.34% → 축소), Δ60d=+2.93%, Δ90d=+4.60% — 반복적 **wrong-sign point estimate** (§3.7 downside-predictive hypothesis 와 opposite direction). CI 모든 horizon 0 가로지름 — sample size 로 해결 안 됨 (point 가 먼저 음수로 flip 해야 CI_upper < 0 가능). sector_limit 141/0 (non-fire 부재 구조적) + leverage_ban 0/141 (MBP backfill 됐으나 variant 미반영). **Codex closure consult** (Q1=(c) synthetic audit path 종료 / Q2=Yes wrong-sign 반복은 meaningful directional evidence / Q3=(ii) moderate §3.7-§3.8 reframe, 25-40 line). **Rigorous diagnosis journey (한 번 오판)**: 처음엔 "leverage_included variant 추가" MBP-local 작업 제안 → 사용자 "엄밀히 그게 맞나요?" 도전 → root-cause 재분석에서 (1) variant 추가 = 통계적 power 증가 아님 (같은 date 의 correlated observation), (2) wrong-sign 은 sample 로 해결 안 됨, (3) § 3.8 closed negative result 가 진짜 finding — Path A (diagnose) + Path D (honest writeup) 조합이 엄밀한 path 로 pivot. **Codex Round 2 HOLD**: 3 over-closure 문구 수정 — (1) "CI_upper < 0 structurally unachievable" → design-bound ("variant ladder × momentum snapshot, us_core 85 tickers, 2026-04-22 data state 에서 point 가 먼저 flip 되지 않는 한 달성 불가, 이 route 의 breadth 확장으로 salvageable 하지 않음"), (2) "measurement 경로 closed" → "본 synthetic-audit 경로 … 이 audit route 는 closed, 상위 §3.7 hypothesis 전체 아님, 다른 경로 (real-portfolio replay, recommendations.outcome 누적, § 2.6 Symmetric amplifier 전용 측정) 는 열려있음", (3) Biggest Risk reproducibility gap — artifact gitignored explicit 명시 + 재현 ssh 명령 + "STRATEGY 본문 trajectory table 이 authoritative". **§3.8 section 5 changes**: 상태 ("closed negative result, 남은 measurement direction 아님") / 실측 결과 (pilot → 60-month trajectory table 병기) / 핵심 finding (repeated directional counter-evidence, opposite-sign) / 사용자 페인 연결 refined (E4-0c plan 무효화, upside-gate hypothesis 는 §3.6 paired counterfactual 과 별도 경로로) / Deferred low-value (leverage_included / GICS backfill / sample 확장 전부 central wrong-sign 해결 못함 → 재활성화 조건 = audit design redesign). **§3.7 section 1 change**: 진단 bullet 추가 — "gates downside-block 방향" 은 structural truth, "effectively downside-predictive 로 작동" premise 는 §3.8 에서 tentatively refuted. **Prudential constraints vs audit-demonstrated predictive gates** 새 paragraph — rule 은 §7 자동 매매 deferred + O'Neil/Minervini lineage 로 justified prudential constraint, §3.8 synthetic audit 은 "forward-downside-predictive gate" 주장을 증명하지 못함. 두 축 혼동하면 "audit validated rule" 잘못된 인용 발생. **Ship**: Phase 1 leveraged ETF backfill (1254 × 4 = 5016 rows MBP, Mac mini 는 production DB 제약으로 backfill 안 함 — 4 variant 에선 leverage 미사용). Phase 2 Mac mini 60-month audit 완료 + migration 22 runtime apply (스케줄러 PID 1098 기존 migration 21 stuck 이었음 — init_db() 호출로 해결). Phase 3 STRATEGY §3.8 rewrite (+35/−22 lines). 3404/3404 test-fast pass, 11/11 doc count. Codex Round 2 GO verdict 후 squash-merge. HEAD c0f3f28 → fbded5e. Archive: `codex-reviews/{PRe4-0b-60month-round1-20260422T022809Z, PR443-round2-20260422T024801Z}.md`. |
| 43 | **E4-0b v2 — SIEGE snapshot-native predictivity audit (methodology fix, codex Plan consult + Round 1 fixes)** | 2026-04-22 | #439 | v1 (#417) 의 48 rows Δ=null failure 재설계. 3 축 invariance (portfolio construction / historical-date audit bias / sector homogeneous) 해결. **Codex Plan consult** (5 Q): Q1-A2 variant ladder (4 templates × N months — momentum_top10 / equal_weight_sample / sector_concentrated / concentrated_top5), Q2-B3 hybrid (binary Δ primary + continuous severity slope secondary), Q3-C1+C3 scope narrow (auditable_now 3 gate 만 — position_limit/sector_limit/leverage_ban), Q4-D1 baseline N=300 target, Q5 **CI_upper < 0** acceptance (codex math correction — NOT CI_lower). Gate eligibility matrix (codex Biggest Risk): auditable_now/audit_incoherent/requires_replayed_state 3 category → §3.8 "11 gate 전부 실측" 오해 차단. leverage_included variant drop (TQQQ/UPRO 0 rows), sector_tilted_tech → sector_concentrated (Unknown exclude). Deterministic RNG (codex Round 1: Python hash() PYTHONHASHSEED 의존 → hashlib.sha256). macro_event_alignment 분류 incoherent → replayable-but-unwired (codex Round 1). **Live 36-month pilot** (144 valid snapshots × 4 variants): position_limit Δ30d=+4.34%, Δ60d=+9.75%, Δ90d=+15.69% — point estimate 모두 양수 but CI crosses 0 → "directional counter-evidence (provisional)", NOT confirmation. sector_limit non-fire 부재로 inconclusive. leverage_ban 측정 불가. STRATEGY §3.8 framing discipline: "§3.7 hypothesis confirmation (partial)" → "limited directional counter-evidence (pilot), inconclusive on sector/leverage". **Codex Round 1**: 4 Medium findings (framing overreach / sector construct validity / non-deterministic seed / macro_event mis-classification) — all fix 됨, no P1 blocker. PR F #437 self-review debt: no retroactive blocker (codex explicit). 27 new regression tests (v1 55 + v2 27 = 82). Backend 82/82 pass. Non-goals (별도 PR): leverage backfill → Mac mini 60-month production run (--save) → E4-0c re-grading. Archive: `codex-reviews/{PRe4-0b-v2-roundplan-20260421T175538Z, PR439-round1-20260421T185704Z}.md`. |

| 37 | **E3-3c — Regime-adaptive position cap wiring (E3 시리즈 final ship)** | 2026-04-19 | #404 | Stage 2 PASS verdict (#402) 의 runtime config + code path 구현. **Codex Round 1 (#402) verdict 엄격 준수**: per_position_max ONLY, sector cap 은 portfolio simulator (E3-4) 후로. **`config/rules.yaml siege_gates.regime_overrides`**: aggressive 1.20× (bull_low_vol, recovery), conservative 0.80× (bear_high_vol, bull_high_vol, stagflation, euphoria), neutral 미등록 = 1.0 default. `regime_override_absolute_cap_pct: 50.0` overflow safety (pension 40% × 1.2 = 48% < 50). **`nuri/trading/engine/certification.py`** 3 helpers: `_current_regime()` (graceful None on classify exception), `_get_position_multiplier(regime)` (YAML lookup, 1.0 fallback at all layers), `_apply_position_multiplier(base, regime)` (cap clip ONLY when raising — lowering preserves base). `_check_position_limits` 가 multiplier 적용 + detail 에 `(regime=X, ×N.NN)` tag (≠ 1.0 일 때만, neutral noise 방지). **Hard veto preserved (orthogonal)**: VIX>30 매수 차단 stays in volatility_gate — 별도 lock-in test 가 threshold = 30 unchanged 검증. E3-3c override scope = per_position_max ONLY — assertion test 가 sector_cap/stop_loss/leverage_ban key 거부. **Live verify** (regime=sideways_high_vol, mult=1.0): position_limit BAC(19.7%) + TSLA(20.5%) FAIL — pre-E3-3c 와 동일 (neutral regime 에서 baseline 보존, regression 0). **25 regression test in 6 class** (codex spec ≥15 충족): TestGetPositionMultiplier(5) + TestApplyPositionMultiplier(7) + TestGetMultiplierGracefulFallback(3) + TestCurrentRegime(3) + TestCheckPositionLimitsWithOverride(5) + TestHardVetoPreserved(2). Codex Round 1 **no P1 findings** + 7 challenge points all 긍정 (defensive fallback OK / cap logic OK / hard veto test 충분 / non-1.0 tagging better / transition deferral OK / perf concern low / bull_high_vol=0.80× defensible). Round 2 skip (lesson #32 — codex 첫 verdict clear, no fix). 3124/3124 test-fast pass (+25 new). Doc counts auto-synced (3122→3147, 143→144). **Known limitations (defer)**: transition grace 없음 (regime flip 시 instant FAIL), max_sector_exposure override (E3-4 portfolio simulator), per-asset-class 차별 multiplier (same reason). **E3 시리즈 6/6 완료** (E3-0/1/2/3a/3b/3c). 사용자 페인 "너무 보수적" 의 mechanical 해결 라인 — bull_low_vol 진입 시 system 이 자동 18% per_position cap 으로 expand. Archive: `codex-reviews/PR404-round1-20260418T181640Z.md`. |
| 42 | **PR F — ATR/regime grid validation infra (codex bubble-bear #6, MARGINAL shadow deferred)** | 2026-04-22 | #437 | 사용자 #1 관심사 "loss rules are why I keep taking losses" 의 structural measurement. **Commit 1**: `nuri/quant/exits/atr.py` — K_GRID (1.5/2.0/2.5/3.0), REGIME_MULTIPLIER (E3-3c parity: bull_low_vol 0.8 / neutral 1.0 / bear_high_vol 1.3), AtrStopResult dataclass + `basis="entry_atr_fixed"` anchor contract lock, `entry_price` explicit required (no default — anchor mismatch 재발 방지, codex Plan Biggest Risk). talib.ATR(14). 15 unit tests in 4 class. **Commit 2**: `scripts/pr_f_atr_validation.py` (E3-3b #402 paired counterfactual 재활용) — us_core 85 tickers × SMA 50/200 golden cross × 12 combo grid × 30/60/90d horizon × walk-forward 2 folds (2024, 2025) × 10000-iter bootstrap CI × 6-metric acceptance (CAGR, MaxDD, Ulcer, Turnover, TaxDrag, HitRate). Baseline -7% (config/rules.yaml stop_loss.per_stock). 18 unit tests (sim math + grid parity lock). **Live validation** (250 trades collected): Top 2 combos k=3.0/regime_mult=1.3 + k=2.5/regime_mult=1.3 paired CI 3 horizon 전부 lower bound > 0 (primary gate PASS), CAGR +13~15pp advantage over baseline 14.44%, turnover −16.7pp, hit rate +10.6pp. **그러나** 6-metric 기준 3/6 (wider stop 의 본질 trade-off: MaxDD −9.21pp worse, Ulcer worse), walk-forward best k shift (fold1 k=3.0, fold2 k=2.5) → robustness 부족. **Verdict: MARGINAL**. **Commit 3**: STRATEGY §3.4 업데이트 — `-7%` 근거를 "O'Neil CAN SLIM inheritance" → "inheritance + repo 5Y 자체 검증 통과 (4+/6 acceptance 달성 combo 없음)" 로 dual justification. Ruff I001 fix + doc count sync (3366→3400, 151→153). **PR F2 로 deferred**: Shadow surface (price_targets.py / `/api/actions` / premarket brief), ATR anchor tracking (entry_date + avg_price 책정일), volume-low `atr_valid` flag, trailing ATR dynamic, frontend 배지. **Codex Plan consult** (E3-3b 재활용 검증 + grid scope frozen): Q1-A (12 combo = 4×3), Q2-A (SHADOW only — urgent gate 변경 금지), Q3-A+B (module `nuri/quant/exits/atr.py` + `compute_indicators` 호출은 PR F2), Q4-A+C (paired + walk-forward), Q5-B+C (bootstrap CI + 6-metric). **Self-review in lieu of codex review** (PR F 는 self-review + 다음 PR 에서 회수 가능 — STRATEGY §2.7 Review phase). 33 new tests (15 ATR + 18 validation). Backend 3400/3400 pass. CI 9/9 required check PASS. Output (gitignored): `data/reports/2026-04-22/pr_f_atr_validation.md` — 재실행 가능 infra. |

| 41 | **PR C — SHADOW crash precursor signals (codex bubble-bear #3, Surface-only)** | 2026-04-22 | #436 | 사용자 "공격적 수익 추구" 성향 ⇒ bear regime 조기 감지 ⇒ drawdown 회피 ⇒ 다음 bull cycle 진입여력 확대. STRATEGY §2.6 "Surface" 단계, action 영향 없음. **Scope**: (a) `nuri/collectors/macro.py` FRED_SERIES 에 `hy_oas: BAMLH0A0HYM2` 추가 + `main(argv)` CLI 추출 (--days N backfill). 기존 `us_3m_yield: DGS3MO` 매핑은 그대로, FRED_API_KEY 설정 시 자동 수집. (b) `nuri/core/signal_config.py` — `is_actionable()` + `list_shadow_signals()` helper. `actionable: false` meta 구조적 격리 trigger. (c) `config/signals.yaml` — 2 SHADOW 신호 등록 (`yield_curve_inversion` Estrella/Mishkin 1998, `hy_oas_widening` Gilchrist-Zakrajsek 2012). 둘 다 `type: SELL` + `actionable: false` + `scope: market_wide`. (d) `nuri/quant/validation/market_signals.py` 신설 — per-ticker `signal_backtest.py` 와 분리된 market-wide detector 모듈. DB read-only, 데이터 부족 시 `fired=False + 데이터 부족` graceful degrade. `MARKET_TICKER="_MARKET_"` sentinel. (e) `nuri/trading/recommend/candidates.py` — SIGNAL_DEFINITIONS loop 맨 위에 `is_actionable` guard — SHADOW 는 candidates 에 절대 포함 안 됨 (codex Plan consult Biggest Risk 대응). (f) `nuri/alerts/premarket_brief.py` — `shadow_signals` context + embed/markdown renderer 에 🌑 SHADOW 섹션. **Codex Plan consult** (5 Q): Q1-A (3m10y, 2s10s 대체 금지), Q2-A (HY OAS 같이 수집), Q3-C-special (breadth 는 PR D 분리 — codex scope cut), Q4-C (`actionable: false` meta, new `type: SHADOW` 피함), Q5-C (승격 DSL 불필요, human judgment + scorecard evidence). **Codex Biggest Risk**: "shadow 를 validated signal 경로 (scorecard tier + candidates) 에 섞어 confidence 에 간접 반영 금지" — `is_actionable` guard 로 structural isolation. **Scope cut**: breadth_divergence_adl 은 PR D 로 defer (per-ticker `signal_backtest.py` 구조 불일치). **20 new tests** (yield_curve 3 + hy_oas 4 + detect_all 2 + actionable meta 5 + candidates exclusion source-level 3 + brief shadow section 3). Backend 3343/3343 pass. **Live verify**: brief stdout 에 "🌑 SHADOW crash precursor (0/2 fired)" + "데이터 부족" detail — FRED_API_KEY 설정 전까지 graceful. Archive: `codex-reviews/2026-04-22/plan-consult-pr-c-*.md`. |
| 40 | **PR B — axis-native read path (`/api/actions` + `/api/dashboard` + `tracker` persist, codex bubble-bear #2)** | 2026-04-22 | #434 | PR A (#429) codex Review caveat 마무리 — "writer discipline 의존" 제거. **Scope**: (a) `nuri/core/axis.py` 신설 — `is_alpha_flat_sell` / `is_alpha_long_buy` / `derive_alpha_action` shared helpers. strict=False (default) back-compat, strict=True 는 PR C 이후 승격. (b) `nuri/api/routes/actions.py:141` — `action == "SELL"` → `is_alpha_flat_sell(...)` 치환. Per-item `alpha_action`/`portfolio_action` 노출. (c) `nuri/api/routes/dashboard.py _get_latest_actions` — SELL 카드는 `is_alpha_flat_sell` True 만, BUY 카드는 `is_alpha_long_buy` True 만. (d) `nuri/trading/recommend/tracker.py` — E-1 candidates + E-2 actions 가 `derive_alpha_action(direction/action)` 으로 axis persist. **Codex Plan consult** (Q1-B dual-accept / Q2-A tracker axis / Q3-C other agents defer / Q4-B SIEGE 분리 / Q5-B Frontend chore). **Scope 확장 (codex Plan Scope 검증)**: 원안 `actions.py` 만 vs 실제 `dashboard.py:248-290` 도 recommendations read path 이므로 포함. **Future writer safety**: concentration-only SELL (alpha_action=None + action="SELL") 을 누군가 emit 해도 reader 는 default-safe — writer 책임 덜 지고 reader 가 alpha axis 명시 없으면 alpha 신호 없음으로 해석. **27 new tests** (axis helper 14 + actions path 4 + dashboard 3 + tracker persist 3 + PR A test 1 update). Backend 3322/3322 pass. **Archive**: codex-reviews/2026-04-22/plan-consult-pr-b-*.md. |
| 39 | **Proactive daily brief — scheduler + user-memory rule (사용자 피드백 "알아서 처리" 구조적 대응)** | 2026-04-22 | #432 | 사용자 세션에서 반복된 피드백 — "왜 뉴스 확인 안 했나" / "비중 조정보다 자산 증식" / "알아서 처리하길 원함" — 의 구조적 원인 제거. **Repo scope**: (a) `nuri/alerts/premarket_brief.py` 신설 (245 lines, DB-only, 기존 `_build_actions`/`_build_opportunities`/`certify`/`classify_regime` 재사용 — codex Plan Q2-A single source of truth). (b) `nuri/scheduler.py` — 1 new job `premarket_brief` at `0 9 * * mon-fri` with `timezone="US/Eastern"` (DST 자동). (c) `nuri/trading/engine/certification.py` — `CallerTag` Literal 에 `"cli:premarket_brief"` + `"cli:session:today"` 등록. (d) `.cspell.json` — 사전 업데이트. (e) Artifact always persist (`data/reports/briefs/{date}.md`, Q4 A+B) — Discord 실패해도 session handoff 가치 보존. **Repo 밖 (user-level memory)**: `feedback_proactive_judgment.md` 신설 + `MEMORY.md` index — judgment intent 감지 시 명시 지시 없이 시스템 + WebSearch 자동 실행 rule. **Codex Plan consult** (5 Q): Q1-A (premarket only), Q2-A (DB-only), Q3-A (repo scope + memory 별도), Q4-A+B (Discord + local always persist), Q5-B (자율 intent 탐지). **Codex Review critical fix**: APScheduler `day_of_week="1-5"` = Tue-Sat (Mon=0 base) → `mon-fri` literal + 3-case behavioral regression (Mon fire / Sat skip / Fri→Mon boundary). **Privacy fix**: real broker identifier (STRATEGY §4.4.1) → `brokerage_alpha` placeholder in fixtures + removed from dictionary. **21 new tests** (context collect 2 + embed 4 + markdown 3 + CLI 3 + scheduler 4 + markdown edge 2 + E2E 3). Coverage 98% (target 87.4%). Backend 3,320/3,320 pass. **Live verify**: `.venv/bin/python -m nuri.alerts.premarket_brief --stdout` produces full brief with regime/SIEGE/4-bucket/opportunities/macro_events/portfolio sections. **Mac mini deploy**: `make deploy-mini` 후 launchd autopull 5분 반영, 첫 자동 fire 는 다음 평일 US/Eastern 09:00 (EDT 기간 KST 22:00). Archive: `codex-reviews/2026-04-21/plan-consult-proactive-brief-{prompt,response}.md` + `review-proactive-brief.md`. |
| 38 | **PR A — alpha_action vs portfolio_action 분리 (codex bubble-bear #1, 재발 차단)** | 2026-04-21 | #429 | 사용자 -₩7M 손실 (2026-04-20: TSLA/AMD/NBIS/NFLX/RKLB SELL 권장 → 다음날 급등) + 2026-04-21 새벽 BAC/TSLA SIEGE REJECT 을 "매도" 로 surface 하려던 trust 위기의 structural fix. Codex bubble-bear research Q6 priority #1: *"The single highest-EV change is split alpha from portfolio/risk actions"*. **Scope**: (a) Migration 22 — `alpha_action`/`portfolio_action` 컬럼 on recommendations (forward-only NULL, legacy row safe). (b) `AgentVerdict` — 두 axis optional 필드 (back-compat). (c) `RiskAgent` — concentration > MAX_SINGLE_POSITION 은 `portfolio_action=REBALANCE` 만 emit, score 영향 0 (이전 `-1` 제거 — codex 지적 "relabel bug" 방지). stop-loss 는 `alpha_action=FLAT`+`score-=3` 유지. (d) Consensus veto — `risk_v.alpha_action=="FLAT"` 만 발동. legacy `action=="SELL"` + `alpha_action=None` 폴백 유지 (non-PR-A agent back-compat). (e) `save_to_recommendations` — 두 axis persist + batch 1회 `classify_regime` (codex Q3 per-batch 권고). (f) `/api/actions` — 새 4번째 `portfolio` bucket (SIEGE position_limit/sector_limit). stop-loss breach 는 urgent 유지 (alpha-driven, § 2.2 mechanical). (g) Frontend `ActionItems` — portfolio bucket sky palette + "리밸런스 권고" copy, back-compat via default prop. **Codex Plan consult** (5 Q): Q1-A (`action` derived retain), Q2-A (frontend 같은 PR), Q3-A (regime per-batch), Q4-A (forward-only NULL), Q5-B (parallel axes, stop-loss dominates legacy SELL). **Live verify** (production DB 2026-04-21 23:00 KST): BAC(19.8%) + TSLA(20.1%) SIEGE violation → `portfolio` bucket (not urgent), urgent 0 (no stop-loss breaches), reason "리밸런스 권고" 언어. **25 new test** (risk_agent+consensus+migration 14 + actions bucket 6 + ActionItems 5). Backend 3299/3299 + frontend 989/989 green. STRATEGY §2.6 Soft penalty (not Hard veto). §3.7 hypothesis → "structural fix shipped" docs-only followup 예정 (PR B/C/D/E/F 와 orthogonal). Archive: `codex-reviews/2026-04-21/plan-consult-pr-a-{prompt,response}.md` + `review-pr-a-*.md`. |

## Tier 2 — 다음 1 달 (P1)

**다음 세션 우선순위** — 구체적 작업 단위로 엄밀 정의 (2026-04-14 재평가).

| 우선 | # | 항목 | 이슈 | 카테고리 | 예상 | Acceptance |
|------|---|------|------|---------|------|------------|
| 🟡 P1 | 1 | **#272 Phase 5 (QA): Negative + Smoke run** | — | test | 1 세션 (네트워크 필요) | 빈 DB/yaml 삭제 negative 3건 + fresh clone → `make setup` → `make universe-sync-us/kr` → `make collect` → `validate_universe` 실행 기록 → `docs/SMOKE_RUN.md` 작성 |
| 🟡 P1 | 2 | **기술분석 통합 to 추천 파이프라인** | — | feat(recommend) | 1-2 세션 | JKHY 에피소드 (2026-04-14) 재발 방지. `nuri/quant/chart_analysis.py` (BB/MACD/RSI) 을 `candidates.py` / consensus에 자동 연동. "fundamentals Buy, technicals Sell" divergence 플래그 추가 |
| 🟡 P1 | 3 | **Universe weekly 1y backfill 자동화** | — | ops | 0.5 세션 | 실측 (2026-04-16): prices 99% 가 이미 3mo+ 보유 (원안 "5d only" 오독 정정). 진짜 gap 은 730/752 ticker 가 1-7일 stale — 기존 `stock_us_night/dawn/stock_kr` 이 `source="portfolio"` 기본값으로 돌아 universe-only ticker 수집 누락. Fix: scheduler 에 주 1회 `source="all"` + `period="1y"` backfill job 2 개 추가 (US/KR). `create_scheduler` 에 `kwargs=job.get("kwargs", {})` 한 줄 누락도 함께 fix. |
| ~~🟡 P1~~ | ~~4~~ | ~~**Earnings quality 분석 통합**~~ | — | ~~feat(recommend)~~ | ~~0.5 세션~~ | **Tier 3 research 로 이관** (2026-04-15). Codex challenge 로 (a) JKHY 실측 surprise 가 4-17% 정상 beat (HARNESS.md §2 에 기록된 "0.0~0.2% soft beat" 는 unit 오독에서 온 문서 오류), (b) literature (Bartov 2002, Kasznik & McNichols 2002, Neururer 2020) 는 meet/beat streak 을 **양의** 신호로 본다. 원안 spec 은 repo-wide unit inconsistency + mature large-cap false positive + literature 반대 방향 문제로 **기각**. 재설계는 Tier 3 "research note" 참조. |
| 🟢 P2 | 5 | **포트폴리오 온보딩 UI (YAML → Dashboard)** | [#25](https://github.com/researcherhojin/nuri-quant/issues/25) | feat(frontend) | 2-3 세션 | 수동 yaml 편집 제거. 2026-04-14 portfolio.yaml 수동 수정 페인포인트 직접 경험 |
| 🟢 P2 | 6 | **OpenBB 호환성 fix** | [#274](https://github.com/researcherhojin/nuri-quant/issues/274) | bug(collectors) | 1 세션 | openbb-core==1.6.7 ↔ openbb-news==1.6.1 충돌로 news/etf_flows 수집 불가. 점진적 upgrade 필요 (콜렉터별 smoke test 후 진행) |
| 🟢 P2 | 7 | **#272 Phase 2c-3 — universe-check 필수 게이트화** | — | ops | 10분 | `make collect-universe` 5/5 PASS 상태 유지 중 → 사용자 수동으로 branch protection required check 토글 |
| ⚪ P3 | 9 | **flaky test 일반 stabilization** | — | test | 1 세션 | #295는 resolved. 다른 flaky 후보 (parallel sys.modules 오염 패턴) 전수 감사 |
| 🟢 P2 | 10 | **Scheduler 로그 노이즈 감축** | — | chore(scheduler) | 0.5 세션 | 2026-04-18 Mac mini 첫 감사 발견. `data/logs/scheduler.err` 1.3MB/40hr (약 30MB/월) — 대부분 apscheduler INFO heartbeat. 3 개 tweak: (a) `logging.getLogger("yfinance").setLevel(logging.WARNING)` 로 401 Crumb / ETF fundamentals 404 같은 transient/expected 노이즈 suppress, (b) events collector 가 ETF ticker 에 대해 fundamentals 호출 skip (fundamental collector 의 ETF skip 패턴 복제 — VOO 404 재발 방지), (c) `RotatingFileHandler` 도입 (scheduler.err/log). 기능 영향 없음, 로그 가독성만 개선. Collector 자체는 정상 — ARK 도 3-tier fallback (cathiesark → ark-funds → yfinance) 으로 복구 확인됨. |

## Tier 3 — 다음 분기 (P2)

큰 작업. 선행 종속성 또는 외부 통합.

| # | 항목 | 이슈 | 카테고리 | 비고 |
|---|------|------|---------|------|
| 1 | **PR #202 commit message Stage 2 history cleanup** | — | security | 사용자 보유 종목 + 손실률이 PR #202 commit message에 노출되어 main git history에 박힘 (TEM/RKLB/PL 등 + PnL). STRATEGY §4.4.1 Stage 2 절차 (GitHub Support 또는 `git filter-repo`) 적용 결정 필요 |
| 2 | **Universe 추가 확장 (Russell 2000)** | — | feat(scanner) | 현재 419 (us_core 85 + us_sp500 254 + kospi200 80). 중소형주 발굴 위해 Russell 2000 (~2,000) 추가 검토 |
| 3 | **Meet/beat streak research spike** (Tier 2 P1 #4 에서 이관) | — | research | 아래 research note 참조 |

### Tier 3 — research note: meet/beat streak in revenue-backed growth

**기각된 원안**: `earnings_surprises` 기반 "soft beat" 플래그 (surprise < 2% 지속 3Q → 성장 stall 경고). FundamentalAgent score 에서 -1.

**기각 이유** (2026-04-15 codex challenge + 실데이터 audit):

1. **Unit 오독**: `docs/HARNESS.md §2` 에 기록된 "JKHY 4Q 연속 +0.0~0.2%" 는 저장 단위 오해. 실측 JKHY Q4'25~Q1'25 = 0.17 / 0.13 / 0.04 / 0.09 (decimal fraction) → **17% / 13% / 4% / 9% 실 beat** 이 맞음. 4-17% 전부 정상 beat 이고 soft beat 아님.
2. **Mature large-cap false positive**: threshold 2% 로 audit 결과 17 개 정상 mature 종목 (ECL, ETN, ABT, LIN, CTAS, CME 등) 이 trigger. Analyst coverage 정밀도 효과 ≠ 성장 stall.
3. **Literature 반대 방향**:
   - Bartov/Givoly/Hayn (2002): meet/beat = premium + 미래 성과 예측. <https://www.sciencedirect.com/science/article/abs/pii/S0165410102000459>
   - Kasznik & McNichols (2002): 꾸준한 meeter = valuation premium. <https://ideas.repec.org/a/bla/joares/v40y2002i3p727-759.html>
   - Neururer / Papadakis / Riedl (2020): 긴 streak = 낮은 ex ante uncertainty. <https://pubsonline.informs.org/doi/10.1287/mnsc.2019.3320>
4. **JKHY 실제 실패 모드**: falling knife (fundamentals 강 + technicals 약). P1 A3 divergence mechanical penalty 가 정확히 잡음 → soft-beat 탐지 불필요.

**Literature-backed pivot (Tier 3 research spec)**:

Neururer (2022) — meet/beat streak 은 **revenue-backed** 성장에서 양의 신호, **expense-backed** 에서는 warning. <https://www.sciencedirect.com/science/article/abs/pii/S106297692200103X>

Research acceptance:
- `earnings_surprises` cohort + 분기별 revenue_growth join
- Expense backing proxy 추가 (gross margin / operating margin trend)
- High-growth (revenue_growth ≥ 20%) universe 에만 적용
- Live universe 에서 backtest:
  - (a) streak 단독
  - (b) streak + revenue-backed filter
  - (c) streak + non-revenue-backed filter (expense engineering)
- Sector/regime 별 안정성 검증
- False positive 순 decision quality 향상 증명 필수

**승격 조건**: 백테스트에서 (b) 가 baseline 대비 +Sharpe/-drawdown 둘 다 유의미 + `pipeline_events` 샘플로 false-positive rate 를 validate 한 후에만 STRATEGY §2.6 Escalation Ladder 의 **Soft penalty** 레벨에 올림.

**참고**: 무작정 소환하지 말 것. 데이터 충분히 축적 (최소 2년 earnings + 4 분기 실시간 backtest) 이후 spike 로.

## 영구 배경 작업 (낮은 우선순위, 발견 시 처리)

| 항목 | 이슈 | 비고 |
|------|------|------|
| TestGate flake on push (PR-only pass) | [#85](https://github.com/researcherhojin/nuri-quant/issues/85) | classify_regime mock leak 수정 완료 (#188). 재발 시 추가 조사 |
| portfolio.yaml 데이터 정합성 모니터링 | — | 수동 매매 후 portfolio.yaml 동기화 필요. 평균가 drift 발견 시 즉시 교정 (사례: PR #204 세션에서 Sub RKLB avg \$60→\$87.7 발견) |
