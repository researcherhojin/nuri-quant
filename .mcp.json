{
  "_comment": "MCP (Model Context Protocol) 서버 — Claude Code/Cursor가 사용할 도구. 추가 시 'mcpServers'에 entry 등록",
  "mcpServers": {
    "nuri-read": {
      "_comment": "Tier 1 비민감 read model (#1306) — SIEGE 판정·buy candidates·macro fact. stdio 전용(바인딩 없음), read-only(PRAGMA query_only). raw 접근이 필요하면 nuri-db(비활성 기본) 대신 이쪽이 기본",
      "command": "uv",
      "args": [
        "run",
        "--no-sync",
        "python",
        "-m",
        "nuri.mcp.server"
      ]
    },
    "nuri-db": {
      "_comment": "SQLite DB 직접 query (read-only 권장). schema 확인, 데이터 검증, ad-hoc 분석용",
      "command": "uvx",
      "args": [
        "mcp-server-sqlite",
        "--db-path",
        "./data/portfolio.db"
      ]
    }
  }
}
