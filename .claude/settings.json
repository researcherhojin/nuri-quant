{
  "permissions": {
    "allow": [
      "Read(**)",
      "Glob(**)",
      "Grep(**)",
      "Bash(make *)",
      "Bash(.venv/bin/* *)",
      "Bash(python -m nuri.* *)",
      "Bash(git status*)",
      "Bash(git log*)",
      "Bash(git diff*)",
      "Bash(git branch*)",
      "Bash(git show*)",
      "Bash(git fetch*)",
      "Bash(git pull --ff-only*)",
      "Bash(gh pr view*)",
      "Bash(gh pr list*)",
      "Bash(gh issue view*)",
      "Bash(gh issue list*)",
      "Bash(jq *)",
      "Bash(curl -s http://127.0.0.1*)",
      "Bash(uv sync*)",
      "Bash(uv run*)",
      "Edit(**)",
      "Write(**/*.md)",
      "Write(**/*.py)",
      "Write(**/*.yaml)",
      "Write(**/*.yml)",
      "Write(**/*.json)",
      "Write(**/*.toml)",
      "Write(**/*.ts)",
      "Write(**/*.tsx)",
      "Write(**/*.sh)",
      "Write(/tmp/**)",
      "Write(/private/tmp/**)"
    ],
    "deny": [
      "Bash(git push --force *)",
      "Bash(git push -f *)",
      "Bash(git reset --hard *)",
      "Bash(git clean -*f*)",
      "Bash(git filter-repo *)",
      "Bash(git filter-branch *)",
      "Bash(git rebase -i *)",
      "Bash(rm -rf *)",
      "Bash(rm -fr *)",
      "Bash(sudo *)",
      "Bash(eval *)",
      "Read(**/.env)",
      "Read(**/.env.local)",
      "Read(**/.env.*.local)",
      "Read(**/*.key)",
      "Read(**/*.pem)",
      "Read(**/*.p12)",
      "Read(**/credentials*)",
      "Read(**/secrets*)",
      "Read(**/id_rsa*)",
      "Read(**/id_ed25519*)",
      "Read(config/kis/*)",
      "Write(**/.env)",
      "Write(**/credentials*)",
      "Write(**/secrets*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -qiE 'git[[:space:]]+push[[:space:]]+([^[:space:]]+[[:space:]]+)*(-f|--force)([[:space:]]|$)|git[[:space:]]+reset[[:space:]]+--hard|git[[:space:]]+clean[[:space:]]+-[a-z]*f([[:space:]]|$)' && { echo '{\"decision\":\"block\",\"reason\":\"파괴적 git 명령 감지 (force push / hard reset / clean -f). 사용자에게 확인 후 진행.\"}'; exit 2; } || true",
            "timeout": 5,
            "statusMessage": "destructive git check..."
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); FILE=$(echo \"$INPUT\" | jq -r '.tool_input.file_path // \"\"'); if [[ \"$FILE\" == */nuri/*.py && \"$FILE\" != */core/db.py ]]; then CONTENT=$(echo \"$INPUT\" | jq -r '.tool_input.new_string // .tool_input.content // \"\"'); if echo \"$CONTENT\" | grep -q 'import sqlite3'; then echo '{\"decision\":\"block\",\"reason\":\"sqlite3는 nuri/core/db.py만 import 가능. query()/query_df()/upsert_*() 사용.\"}'; exit 2; fi; fi",
            "timeout": 5,
            "statusMessage": "sqlite3 import guard..."
          },
          {
            "type": "command",
            "command": "INPUT=$(cat); [ -x .venv/bin/python ] && [ -f scripts/check_privacy_leak.py ] || exit 0; CONTENT=$(echo \"$INPUT\" | jq -r '.tool_input.new_string // .tool_input.content // \"\"'); [ -z \"$CONTENT\" ] && exit 0; OUT=$(echo \"$CONTENT\" | .venv/bin/python scripts/check_privacy_leak.py --message --quiet 2>&1); if [ $? -ne 0 ]; then printf '{\"decision\":\"block\",\"reason\":\"Privacy ticker+PnL leak detected (PR #202 signature). Findings: %s. See scripts/check_privacy_leak.py + STRATEGY §4.4.1. Use placeholders or round values.\"}' \"$(echo \"$OUT\" | head -3 | tr '\\n' ' ' | sed 's/\"/\\\\\"/g')\"; exit 2; fi",
            "timeout": 5,
            "statusMessage": "privacy ticker+pnl check..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if [[ \"$f\" == *.py ]]; then .venv/bin/ruff check \"$f\" --no-fix 2>&1 | head -10; fi; }",
            "timeout": 15,
            "statusMessage": "ruff check..."
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if [[ \"$f\" == */nuri/*.py ]] && grep -qn 'datetime\\.now()' \"$f\" 2>/dev/null; then echo '{\"decision\":\"block\",\"reason\":\"datetime.now() 금지 — kst_now() 또는 today_kst() 사용. See nuri/core/timezone.py\"}'; exit 1; fi; }",
            "timeout": 5,
            "statusMessage": "datetime.now() check..."
          }
        ]
      }
    ]
  }
}
