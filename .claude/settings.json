{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if [[ \"$f\" == *.py ]]; then cd /Users/ehbebe/workspace/nuri-quant && .venv/bin/ruff check \"$f\" --no-fix 2>&1 | head -10; fi; }",
            "timeout": 15,
            "statusMessage": "ruff check..."
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if [[ \"$f\" == *.py ]] && grep -qn 'datetime\\.now()' \"$f\" 2>/dev/null; then echo '{\"decision\":\"block\",\"reason\":\"datetime.now() 금지 — kst_now() 또는 today_kst() 사용. See nuri/core/timezone.py\"}'; exit 1; fi; }",
            "timeout": 5,
            "statusMessage": "datetime.now() check..."
          }
        ]
      }
    ]
  }
}
