---
description: SIEGE predictivity audit (E4-0b v2) 재실행. **수동 호출 전용** (skill 자체는 `disable-model-invocation: true` — 자동 trigger 차단). 60-month rerun, gate-level Δ 해석, variant ladder 추가 시만 사용. STRATEGY §3.8 closed route — 새 plan consult 없이 변경 금지. Usage `/nuri-siege-audit`.
---

`nuri-siege-audit` 스킬을 invoke 하라. `.claude/skills/nuri-siege-audit/SKILL.md` 의 재현 절차 + v2 methodology + 60-month gate-level 결과 + 재활성화 조건 참고.

**주의 (필독)**:
- 이 audit route 는 2026-04-22 60-month rerun 에서 acceptance 미달 (point estimate wrong-sign). 단순 재실행은 의미 없음.
- 재활성화는 §3.8 의 3 가지 조건 중 하나 충족 시만 (actual-portfolio replay / Symmetric amplifier 이후 upside-only / audit design 근본 redesign).
- `prudential` vs `predictive` 축 분리 규칙 위반 금지 — rule 인용 시 prudential 근거로만 사용.
