<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Nuri-Quant 스케줄러 launchd 설정.

  설치:
    cp scripts/com.nuri-quant.scheduler.plist ~/Library/LaunchAgents/
    launchctl load ~/Library/LaunchAgents/com.nuri-quant.scheduler.plist

  제거:
    launchctl unload ~/Library/LaunchAgents/com.nuri-quant.scheduler.plist
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.nuri-quant.scheduler</string>
    <key>ProgramArguments</key>
    <array>
        <string>/Users/ehbebe/workspace/nuri-quant/.venv/bin/python</string>
        <string>-m</string>
        <string>nuri.scheduler</string>
    </array>
    <key>WorkingDirectory</key>
    <string>/Users/ehbebe/workspace/nuri-quant</string>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <!-- fd 한계 상향(기본 256). yfinance 내부 SQLite 캐시(tkr-tz.db/cookies.db)+
         HTTP 세션 fd 누수가 장수 데몬에서 256 천장을 쳐 sqlite3 'unable to open
         database file' + heartbeat 멈춤을 유발(2026-06-22). 헤드룸 확보용 완화책 —
         누수 자체는 watchdog 자동 재시작으로 회수. -->
    <key>SoftResourceLimits</key>
    <dict>
        <key>NumberOfFiles</key>
        <integer>4096</integer>
    </dict>
    <!-- §3.11 원장 단일 (#856): adjudication 원장은 이 머신(Mac mini) DB 뿐이고
         MBP 는 read replica 다. `NURI_ROLE=production` 일 때만 월간 alpha 진행
         리포트가 #brief 로 stage 된다.
         왜 `.env` 가 아니라 여기인가: deploy_to_mini.sh 3단계가 MBP 의 `.env` 를
         mini 로 SCP 하므로, mini `.env` 에만 넣으면 다음 배포에 조용히 지워진다.
         plist 는 동기화 대상이 아니고 프로덕션 실행 컨텍스트 그 자체다. -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>NURI_ROLE</key>
        <string>production</string>
    </dict>
    <key>StandardOutPath</key>
    <string>/Users/ehbebe/workspace/nuri-quant/data/logs/scheduler.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/ehbebe/workspace/nuri-quant/data/logs/scheduler.err</string>
</dict>
</plist>
