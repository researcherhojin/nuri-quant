<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<!--
  com.nuri-quant.health-check (#529 Phase 1, SRE-Incident-Agent precursor)

  매시간 health_check.sh 실행. exit code 1/2 시 stderr 로그가
  data/logs/health_check.err 에 남음.
  ⚠️ 이 plist 는 **설치하지 않는다** (#939). 고유 검사 3종(schema version / 필수 테이블 /
  writer role)은 SREIncidentAgent detector 로 이식됐고 그쪽이 log_incident + Discord
  publish 경로를 탄다. 예전 주석은 'SRE-Incident-Agent 가 이 로그를 watch' 라고 적었으나
  그런 코드는 존재한 적이 없다 — 광고된 배선이 허구였다. 스크립트 자체는 Discord /health
  슬래시 명령의 on-demand 실행 경로로 남는다.

  설치:
    설치 (권장): make crons-install   # 치환 + 원자 교체를 대신 해준다

    손수 할 때. bare cp 금지 — #980 이후 repo plist 는 `/Users/USER/` 플레이스홀더를
    담아서, 그대로 복사하면 launchd 가 exit 78 (EX_CONFIG) 로 죽는다 (#988). 게다가
    stderr 에 아무것도 안 남고, 돌던 job 은 캐시된 정의로 살아남아 다음 재시작까지
    잠복한다. `&&` 로 묶어 치환 실패가 load 로 이어지지 않게 한다.
      N=com.nuri-quant.health-check.plist
      sed "s|/Users/USER/|$HOME/|g" scripts/launchd/$N > ~/Library/LaunchAgents/$N.tmp \
        && mv ~/Library/LaunchAgents/$N.tmp ~/Library/LaunchAgents/$N \
        && launchctl load ~/Library/LaunchAgents/$N

  주의: PROGRAM_PATH 는 사용자 환경에 맞게 수정 필요 (USER 자리).
-->
<dict>
    <key>Label</key>
    <string>com.nuri-quant.health-check</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>-c</string>
        <string>cd /Users/USER/workspace/nuri-quant &amp;&amp; bash scripts/ops/health_check.sh</string>
    </array>

    <key>WorkingDirectory</key>
    <string>/Users/USER/workspace/nuri-quant</string>

    <!-- 1시간 간격. -->
    <key>StartInterval</key>
    <integer>3600</integer>

    <key>RunAtLoad</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/Users/USER/workspace/nuri-quant/data/logs/health_check.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/USER/workspace/nuri-quant/data/logs/health_check.err</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
    </dict>

    <key>KeepAlive</key>
    <false/>
</dict>
</plist>
